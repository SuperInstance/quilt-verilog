# ROUND 22 — regime-membership map + the 2pd=6 double-duty probe (pre-registered 3e9a3a7, STUDENT nudge): the mechanism story dies, the coincidence demotes, and the grid is flatter than anyone predicted

**Verdict: both pre-registered gates fired, both against the interesting story — booked as fired.**

## M-K2: K=2 membership — pre-declared mechanism FALSIFIED

Pre-declaration: K=2 should join the quarter-power regime because pulse interference needs ≥2 candidate streams. Measured: **K=2 is Δ-flat** (walls 4, 4 at Δ=8/16) — the gate books FLAT, so the interference-threshold story **dies**. The line between the flat/stratified world and the rest sits **between K=1 and K=2**, not at K=4 as round 21's naming ("K≥4 regime") implied.

## M-PD4: the 2pd=6 double-duty question — DEMOTED

At K=8/Δ=12: pd=3 → wall 3, pd=4 → wall 3. **The comp regime extends through pd=4**, so the boundary pd* ∈ (3,4] and 2pd* ∈ (6,8] does **not** contain 6. Per the pre-registered rule: **6 is NOT doing double duty** — STUDENT's structural-link candidate is answered as **coincidence** (for the comp-regime edge, at mid Δ). Ledger entry recorded; if a later sweep ever shows the edge pinned at exactly pd=3 with Δ resolved, this entry gets revisited — the demotion is Δ=12-scoped.

## The surprise: at Δ=12 the whole grid is structureless

Every (K ∈ {2,4,8}, pd ∈ {2,3,4,6}) cell reads wall 3–4 (K=2/4 → 4, K=8 → 3), zero pd-stratification anywhere (gaps 0/0/0). Round 21's pd=6 blowout (walls 6–8) and pd-stratified K=1 seating (6/9/none) were measured at **large or probed Δ** — the "pd-stratification" is **Δ-carried**, not a fixed (K,pd) property. Booked reading: the regime axes are **Δ (carries the wall growth and the pd=6 blowout)** and **K=1-vs-K≥2 (carries the step 6/9/none vs 3–4)**; pd acts only through Δ-interactive terms. The STUDENT-style one-line operator rule this buys:

> At Δ≤16 and K≥2 you are in the flat regime (wall 3–4, apply nothing fancier); the quarter-power F2 is a large-Δ/K≥4 description; K=1 is its own pd-stratified world.

## Gates fired honestly

- M-K2 → FLAT → pre-declared mechanism falsified (no rescue clause; the K=2 cell is F2-close at Δ=12 but the flatness rule checked first, as frozen).
- M-PD4 → EXTENDS → coincidence demoted.
- Canary: octave replays 2/3/4 exact PASS.

## Next rung (named)

Δ-resolved map at K=2 vs K=1: is K=2 flat at *all* Δ or only ≤16 (does it cross to F2 at Δ≥24)? If K=2 never crosses, the K-line is a true phase boundary between single- and multi-stream worlds, and the K=1 seating table is the only object needing a pd-stratified model.

Raw: `r22-regimemap-output.txt` (90 s; 14 cells + 3 replays, 5 seeds, N 2..13, comp arm).
