# ROUND 23 — Δ-resolved K=2-vs-K=1 map (pre-registered f95fcf8): FLAT per the frozen gate, with a flagged symmetric uptick at d=96

**Verdict: gate M-K2CROSS fires FLAT — K=2 stays within ±1 of its anchor (walls 4,4,4,4,4,4 then 5 at d=96) — so under the rule frozen before the run, the quarter-power regime boundary moves UP to K≥4 and round-21's naming is restored.** Booked alongside: the K=1 6-seat plateau holds through d=64 (6,6,6,6,6) with wall 7 at d=96 — below the pre-declared ≥8 growth line, so K=1 stays "flat" too, with the same +1 tail cell.

## The flagged observation (no rescue clause — this is a next-rung pointer, not a verdict edit)

Both maps show **exactly +1 at d=96 and perfectly flat before it**. For K=2, the d=96 wall of 5 *equals* the F2 extrapolation (⌈2·48^0.25⌉ = 5): the cell is consistent with the onset of the quarter-power world at the largest probed Δ — or with a +1 wobble at the resolution edge. The frozen classifier cannot distinguish these, and per pre-reg it fires FLAT. Named next rung: Δ ∈ {128, 192} at K=2 (and K=1) — if the +1 becomes +2, the stream-count line dissolves into a Δ-delayed cross and both K worlds share one slow law; if it reverts to 4, the flat regime is real at all probed Δ and the K≥4 quarter-power stands as the lone r-law.

## World picture after rounds 19–23

- **K≥4, pd≤3, large Δ**: quarter-power comp wall F2 = ⌈2·r^0.25⌉ (round-21 banked form).
- **K=2**: flat at 4 through Δ=64 — behaves like the multi-stream world with the interference already saturated, *not* like a single stream.
- **K=1**: pd-stratified step (6/9/none at pd 3/4/6), Δ-flat through 64 — single-stream world confirmed distinct.
- **Δ=12 slice**: structureless everywhere (round 22) — pd acts only through Δ-interactive terms.

Canaries: K=8 octave replays 2/3/4 exact; K=1 d=8 seat 6 exact. Raw: `r23-k12map-output.txt` (82 s).
