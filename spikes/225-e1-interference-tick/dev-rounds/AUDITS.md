# AUDITS — cross-repo fact-check + cross-pollination ledger (branch g3-kinduction)

Created 2026-09-03 at round-7 retry: this file did not exist when the retry
lane opened — the cron lane's round 7 committed to elephant (`79dcd98`) but
failed before booking here. Entries below follow the ROUNDS.md block format:
round #, repo, what was checked, verdict, commit hash.

## Round 7 (retry) — elephant fact-check + cross-pollinate — 2026-09-03 ~14:2x–14:4x AKDT
- **Repo:** SuperInstance/elephant (JEPA room-temperature sense), default branch `main`. Red line respected: repairs + clearly-marked notes only, no restructuring.
- **Links:** 20 relative links across README + docs/*.md checked mechanically; 0 anchors in use; 1 regex false positive (`t−t1` — quadratic-interpolant math, not a link). **0 real broken links, 0 fixed.** Sibling repos all live via ls-remote: collective-unconscious, crab-traps, fleet-radio, AI-Writings, mud-arena, quilt, quilt-rust, plato-vision-jepa. `quilt-rust/docs/{field-edge-ledger-bridge,fleet-as-fractal-jepa}.md` confirmed on main (raw 200). CI (`.github/workflows/ci.yml`) healthy, no stale refs.
- **Claims verified by re-run:** 393/393 tests in ~37s across 31 test files (README's re-verified numbers exact); quickstart field numbers exact (warmth +0.29/κ 2.04 vs −0.05/1.96, distance 0.83, gap +0.34); tapnight divergence `0.389 → 0.859` exact; `field.py` API surface (warmth/concentration/distance/sauna_plunge_gap/acclimation_curve/charisma_pull) all present; `vmf.py::kl_sym` present.
- **Claims flagged + fixed:** (1) module table said DEFAULT_DIALS = "eight-dial bank" — it is **nine** (vision dial included; verified by import). (2) "all 31 `.py` files" — recursive count is **41** (31 top-level + 10 under `dials/`); count note made precise. (3) `docs/radasound-crossover-2026-08-22.md` claim "vMF gate 1 closed 2026-08-19" — **no gate-1 referent exists** in the repo's own gate chain (named gates: gate-2 = `data/nights/` measurement corpus; gate 3 = registered eval, text-tier PASS in-sample 3/3, held-out **FAIL 1/3 seeds** per `CONTRAST-HELDOUT-ROOM-2026-08-19.md`); audit note added in place naming the real chain — claim not deleted, boundary booked.
- **Cross-pollination applied:** README "Cross-pollination — quilt-verilog doc disciplines" section (clearly dated/marked): honest-boundary booking (headline claims tagged **PROVEN** — tests/quickstart/tapnight re-runs; **MODELED** — 30-day fleet-sim arc, deterministic demo not field data; **SPECULATED** — v1/v3 learned designs, held-out FAIL booked); tapestry trail-presentation (held-out FAIL + addendum ruling + DIAGNOSIS linked as first-class results); archive-by-rename (dated correction notes over silent rewrites, per the module-table recount pattern).
- **Commits:** cron lane `79dcd98` (README staleness: 277/25→393/31, module count, numbers — re-verified by this retry), retry lane `bf7380d` (dial-bank count, 41-file precision, gate-1 audit note, cross-poll section). Pushed to `main`, no force, no deletions.
- **Verdict:** CLEAN-with-repairs — repo's evidence hygiene is strong (pre-registration chain, frozen baselines, honest FAIL docs); the one soft spot was an unresolvable gate citation, now booked.
